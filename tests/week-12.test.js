const fs=require('fs'),p='mes-03-proyecto/semana-12-deploy-presentacion/solution/README.md';if(!fs.existsSync(p)){console.error('❌ Falta README.md final');process.exit(1)}const s=fs.readFileSync(p,'utf8').toLowerCase();const req=['demo','funcionalidades','tecnologías','instalación','testing','mejoras'];const bad=req.filter(x=>!s.includes(x));if(bad.length){console.error('❌ README final incompleto:',bad);process.exit(1)}if(!/https?:\/\//.test(s)){console.error('❌ Agrega URL de deploy');process.exit(1)}console.log('✅ Semana 12 OK');