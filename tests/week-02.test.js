const fs=require('fs'),p='mes-01-fundamentos-profesionales/semana-02-javascript-moderno/solution/index.js';if(!fs.existsSync(p)){console.error('❌ Falta solution/index.js');process.exit(1)}const m=require('../'+p);const req=['obtenerActivos','obtenerNombres','buscarPorId','calcularTotal','aplicarDescuento'];const bad=req.filter(k=>typeof m[k]!=='function');if(bad.length){console.error('❌ Faltan funciones:',bad);process.exit(1)}const users=[{id:1,name:'Ana',active:true},{id:2,name:'Luis',active:false}];if(m.obtenerActivos(users).length!==1||m.buscarPorId(users,2)?.name!=='Luis'){console.error('❌ Revisa comportamiento de funciones');process.exit(1)}console.log('✅ Semana 2 OK');