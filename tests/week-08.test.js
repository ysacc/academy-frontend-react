const fs=require('fs'),base='mes-02-react/semana-08-typescript-arquitectura-testing/solution/src/';let bad=[];for(const f of ['types.ts','ProductCard.tsx','ProductCard.test.tsx'])if(!fs.existsSync(base+f))bad.push(f);if(!bad.length){const all=['types.ts','ProductCard.tsx'].map(f=>fs.readFileSync(base+f,'utf8')).join('\n');if(/:\s*any\b/.test(all))bad.push('evita any');const t=fs.readFileSync(base+'ProductCard.test.tsx','utf8');if(!/(test|it)\s*\(/.test(t))bad.push('test de comportamiento');}if(bad.length){console.error('❌ Semana 8:',bad);process.exit(1)}console.log('✅ Semana 8 OK');