import { useState } from 'react';

export default function App() {
  const [form, setForm] = useState({ query: '' });
  // TODO: validación, submit, loading, error, success.
  return <main>{/* TODO */}</main>;
}
