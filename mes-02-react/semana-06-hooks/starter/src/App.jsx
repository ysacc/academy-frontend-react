import { useState } from 'react';

const items = ['React', 'TypeScript', 'Testing', 'Git', 'APIs'];

export default function App() {
  const [query, setQuery] = useState('');
  // TODO: deriva la lista filtrada sin crear estado duplicado.
  return <main>{/* TODO: input controlado + lista */}</main>;
}
